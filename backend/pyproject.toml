[project]
name = "todo-app-backend"
version = "0.1.0"
description = "To-Do App Backend with FastApi"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn>=0.27.0",
    "pydantic>=2.5.0",
]
